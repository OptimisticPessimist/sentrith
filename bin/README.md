# bin/

Place the prebuilt `sentrith` binary for the current platform here.

Unix:

```text
bin/sentrith
```

Windows:

```text
bin/sentrith.exe
```

Do not commit platform binaries to the template repository unless you intentionally want vendored binaries.
Prefer GitHub Release assets.
